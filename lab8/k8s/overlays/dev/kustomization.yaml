apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

namePrefix: dev-

commonLabels:
  environment: dev

resources:
  - ../../base

patches:
  - path: flask-deployment-patch.yaml
  - path: flask-env.yaml