apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - redis-deployment.yaml
  - redis-service.yaml
  - flask-deployment.yaml
  - flask-service.yaml

commonLabels:
  app.kubernetes.io/part-of: flask-redis-app
